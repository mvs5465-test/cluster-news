apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-news.fullname" . }}
  labels:
    {{- include "cluster-news.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-news.selectorLabels" . | nindent 4 }}
